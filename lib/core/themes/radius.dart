import 'package:flutter/material.dart';

final class AppRadius {
  AppRadius._();

  static const borderRadiusSmall8 = BorderRadius.all(Radius.circular(8));
  static const borderRadiusMedium12 = BorderRadius.all(Radius.circular(12));
  static const borderRadiusLarge24 = BorderRadius.all(Radius.circular(24));
}
